---
import { useState } from 'react';
---

<script>
  let isVisible = true;
  function closeAd() {
    isVisible = false;
  }
</script>

{isVisible && (
  <div class="relative w-full bg-gray-100 p-4 mb-4 flex justify-center">
    <button 
      class="absolute top-2 right-2 bg-red-500 text-white rounded-full w-6 h-6 flex items-center justify-center" 
      on:click={closeAd}
    >
      ✕
    </button>
    <img 
      src="/ruta-de-tu-imagen.jpg" 
      alt="Publicidad" 
      class="max-w-full h-auto rounded-lg"
    />
  </div>
)}

<div>
  <h1>Contenido Principal</h1>
  <p>Este es el contenido que se ajustará hacia arriba cuando se cierre la publicidad.</p>
</div>